eureka:
  client:
    server-url:
        defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-adress: true

spring:
  datasource:
    username: ruddram
    password: jLYIKFLjheDA2ubPEC0AkaUx9qHOkrGP
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,beans,env,loggers
    health:
      show:
        details: always
